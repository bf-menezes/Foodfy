{% extends "layout.njk" %}


{% block head %}
    
<title>recipeslist</title> 

{% endblock head%}


{% block main %}

<section class="recipeslist-container">

    {% for recipe in recipes %}

    <div>
        <a href="/recipedetails/{{ loop.index0 }}"><img src="{{ recipe.image }}" alt="{{ recipe.title }} Image"></a>
        <h3>{{ recipe.title }}</h3>
        <p>por {{ recipe.author }}</p>
    </div>
    
    {% endfor %}
    
</section>                  

{% endblock main %}

